<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hamburgcodingschool.dagger2">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:name=".e19android.AndroidApplication"
      android:supportsRtl="true">
    <!-- android:name=".complex.application.ComplexApplication" -->
    <activity android:name=".e18complex.home.HomeActivity"/>
    <activity android:name=".e18complex.login.LoginActivity"/>
    <activity android:name=".e18complex.logout.LogoutActivity"/>
    <activity android:name=".e18complex.MainActivity">
      <!--<intent-filter>-->
        <!--<action android:name="android.intent.action.MAIN"/>-->
        <!--<category android:name="android.intent.category.LAUNCHER"/>-->
      <!--</intent-filter>-->
    </activity>
    <activity android:name=".e19android.SomeActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
