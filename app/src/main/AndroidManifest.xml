<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hamburgcodingschool.dagger2">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:name=".complex.application.ComplexApplication"
      android:supportsRtl="true">
    <activity android:name=".complex.home.HomeActivity"/>
    <activity android:name=".complex.login.LoginActivity"/>
    <activity android:name=".complex.logout.LogoutActivity"/>
    <activity android:name=".complex.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
